targets:
  $default:
    sources:
      - lib/**
      - .env
      - $package$
    builders:
      json_serializable:
        options:
          explicit_to_json: true
          field_rename: snake
        generate_for:
          - lib/data/**/*.dart
      retrofit_generator:
        generate_for:
          - lib/data/**/*.dart
